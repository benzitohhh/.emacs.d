# -*- coding: utf-8 -*-
# name: panda dataframe example, with a plot
# expand-env: ((yas-indent-line 'fixed))
# --
rng = pd.date_range('2013-01-01', periods=10, freq='D')
df = pd.DataFrame(np.random.randn(10, 2), columns=['A', 'B'], index=rng)
df.plot()
plt.show()
